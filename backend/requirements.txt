# Backend dependencies. Rebuild the venv on a fresh pull with:
#   python3 -m venv venv && source venv/bin/activate && pip install -r requirements.txt
fastapi[standard]==0.139.2
simpleobsws==1.4.3
twitchAPI==4.5.0
